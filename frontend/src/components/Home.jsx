import React, { useState } from 'react';

export default function Home() {
    // TODO: Create state for 'playerName' and 'roomIdInput'

    // TODO: Build a UI with:
    // 1. An input for the player's name
    // 2. A button to "Create Room" (emits 'create_room' event)
    // 3. An input for a Room ID
    // 4. A button to "Join Room" (emits 'join_room' event)

    return (
        <div>
            <h1>Draw & Guess Lobby</h1>
            {/* UI implementation goes here */}
        </div>
    );
}
